security:
    firewalls:
        main:
            anonymous: true
            http_basic: true

    encoders:
        Symfony\Component\Security\Core\User\User: 'plaintext'

    providers:
        main:
            memory:
                users:
                    bob: { password: pa$$word, roles: ROLE_USER }
